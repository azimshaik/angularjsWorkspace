import { Component } from '@angular/core';

@Component({
  moduleId:module.id,
  selector: 'httpcalls',
  templateUrl: `httpcalls.component.html`,
})
export class HttpcallsComponent  { name = 'Angular'; }
